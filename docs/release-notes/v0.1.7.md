# Fluent 串口助手 v0.1.7

这是一个自动化调试、常用包管理和侧边栏体验增强版本。

## 新增

- 终端搜索支持保留原记录并高亮命中内容，可上/下跳转匹配，支持大小写敏感、正则搜索和方向过滤。
- 常用包支持分组、备注、启用/停用、批量发送、排序和 JSON 导入导出。
- 增加宏命令能力，支持多步骤发送、步骤间延时、等待文本/HEX 响应、循环运行、失败中止和 CSV 结果导出。
- 增加自动应答能力，支持文本、HEX、正则匹配，命中后可立即或延时发送文本/HEX 应答。
- 自动应答发送记录会标记来源，CSV 导出同步包含来源列，便于区分手动发送和自动应答。

## 变更

- 更新检查信息条调整到顶部显示，避免遮挡主要操作区域。
- README 截图改为动态演示图。

## 修复

- 修复左侧卡片内部控件获得鼠标滚轮时，侧边面板无法继续滚动的问题。

## 验证

- `cmake --build build/codex-debug --target format`
- `cmake --build build/codex-debug --parallel`
- `cmake --build build/codex-release --target format-check`
- `cmake --build build/codex-release --parallel`
- `git diff --check`
- `ctest --test-dir build/codex-debug --output-on-failure`
