# Fluent 串口助手 v0.1.8

这是一个多会话、实时绘图、自动日志和多语言体验增强版本。

## 新增

- 自动日志增强为 TXT、CSV、BIN 三种格式，可按单文件大小滚动，日志文件名包含时间、串口名和波特率，并在侧栏显示当前写入状态。
- 增加多标签会话，标签放在标题栏顶部，每个会话拥有独立串口、终端、发送区、统计和日志配置，新标签会复制当前会话配置。
- 增加快速绘图独立窗口，可从接收文本按全部数字、分隔值、键值对和 JSON 对象协议取数，支持多通道实时曲线、协议示例提示、暂停、清空、复位视图和 CSV 导出。
- 增加应用中英文翻译资源和语言设置，支持简体中文、English、跟随系统，并可在标题栏快捷按钮中无需重启切换。
- 增加标题栏快捷操作，搜索、快速绘图、语言、接收区 HEX/文本、主题和设置入口集中放在统计信息右侧。

## 变更

- 终端搜索改为弹出悬浮窗口，减少终端标题区域占用。
- 终端统计信息靠左、快捷图标靠右，标题栏标签和 logo 间距更稳定。
- 发送区增加 HEX/文本快捷切换，接收区增加 HEX/文本图标切换。
- 侧栏表单英文布局改为统一标签列宽，并压缩部分英文短文案，避免切换英文后控件显示不全或上下不对齐。

## 修复

- 修复标题栏标签在新增标签、切换主题或切换标签后高度、底部强调色和显示区域回退的问题。
- 修复标题栏 logo 在多标签会话布局中消失的问题。
- 修复新增快捷按钮垂直不居中和文本按钮在标题栏高度内显示不稳的问题。
- 快速绘图曲线右键菜单改为中文显示。

## 包含提交

- `487b34e` feat: 增强自动日志功能，支持多种格式和文件大小限制
- `a97618b` feat: 添加工作台会话页面，支持多标签会话和会话配置复制功能
- `2563957` feat: 添加快速绘图功能，支持多通道实时曲线显示
- `6724c09` feat: 增强工作台页面标题栏功能，支持动态显示标签
- `933b660` feat: 添加国际化支持，整合翻译功能
- `73d75aa` feat: 更新翻译文件，增强多语言支持

## 验证

- `cmake -S . -B build/release-verify -G Ninja -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_PREFIX_PATH=C:/Qt/6.11.1/mingw_64 -DCMAKE_CXX_COMPILER=C:/Qt/Tools/mingw1310_64/bin/g++.exe -DCMAKE_MAKE_PROGRAM=C:/Qt/Tools/Ninja/ninja.exe`
- `cmake --build build/release-verify`
- `ctest --test-dir build/release-verify --output-on-failure`
- `git diff --check`
